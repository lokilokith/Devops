- `[ ]` Fix Dark Mode in `frontend/src/App.tsx`
- `[ ]` Expose `audit` and `notifications` models to Alembic
- `[ ]` Generate missing Alembic migration
- `[ ]` Update `app/audit/repository.py` to remove `commit()` and `rollback()`
- `[ ]` Update `app/notifications/repository.py` to remove `rollback()`
- `[ ]` Update `app/audit/service.py` to raise exceptions
- `[ ]` Update `app/notifications/service.py` to raise exceptions
- `[ ]` Write regression tests in `tests/integration/test_approval_workflow_atomicity.py`
- `[ ]` Run automated tests (`pytest --cov=app`)
- `[ ]` Rebuild Docker containers
- `[ ]` Manual browser verification
- `[ ]` Create final report
